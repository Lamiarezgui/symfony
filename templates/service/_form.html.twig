
{{ form_start(form) }}
<div class="form-group">
    {{ form_widget(form) }}</div>
    <button  class="btn btn-success">{{ button_label|default('Save')}} </button>
    <a href="{{ path('service_index') }}"style="margin-left:10px"><i class='fas fa-undo'></i></a>
    
{{ form_end(form) }}
